import React from 'react';
import {Box, Stack, Typography} from "@mui/material";

const EventCard = () => {
    return (
        <Stack>
                card
        </Stack>
    );
};

export default EventCard;
